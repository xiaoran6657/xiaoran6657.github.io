# 项目状态

更新：2026-09-11

## 当前范围

首页、作品索引、XUILab 概览、三个技术案例和关于页面，均有中英版本。另含根路径入口和 404，共 16 个 HTML 页面。主题支持自动/浅色/深色；身份、职业定位、邮箱与简历可配置。

已镜像 XUILab v0.1.0-preview.1 的 12 张原图和 3 段视频，源文件 SHA-256 均通过核对。原始素材清单的 Git blob 与公开 tag 一致。

## 已完成验证

- 本地 Astro 类型检查：0 errors / warnings / hints。
- 生产构建：16 HTML 页面与 sitemap。
- 静态验证：路由、站内资源/锚点、页面语言、元数据、图片描述/尺寸、视频非自动播放策略、15 个媒体哈希。
- 本地首页 HTTP 请求成功，已提交用户预览打开请求。
- 生产脚本测试通过：系统主题、手动覆盖、持久化、中英切换、锚点/查询保留与存储禁用回退。

## 待完成

- 本地 Git 已初始化；正在创建/连接 GitHub 仓库并启用 Pages。
- 确认远端构建部署结果与在线入口。

## 验证边界

尚未执行真实浏览器截图、移动端布局、键盘交互和视频播放/拖动验收；构建和脚本模拟测试不能替代这些。本站没有重新运行 XUILab 实验。

## 后续维护

1. 个性化内容只改 src/data/profile.ts 中希望公开的字段。空邮箱/简历不显示。
2. 编辑案例时同步维护中英文和来源；升级上游版本时重新核对媒体与结论。
3. npm run verify 通过后提交；main 推送将自动部署，PR 仅构建验证。
